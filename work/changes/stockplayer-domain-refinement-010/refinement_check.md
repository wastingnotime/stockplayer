# Refinement check

Slice 010 confirms that multiple buys and sells produce an explainable cost
basis and realized result. Floor rounding is explicit, deterministic, and the
remaining cost retains the unallocated minor-unit remainder.

The next refinement should add unrealized result from simulated current prices
or sell-side quantity reservations, based on the next architecture lesson.
