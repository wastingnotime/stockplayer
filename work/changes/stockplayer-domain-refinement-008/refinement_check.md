# Refinement check

Slice 008 confirms that ledger, cash, reservation, and position projections are
recoverable from event history. A fresh projection matches the incremental
projection after a partial execution.

The next refinement can expose scenario evidence and invariant checks around
projection recovery before adding API or browser adapters.
