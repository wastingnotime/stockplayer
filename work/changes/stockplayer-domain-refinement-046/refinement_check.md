# Refinement check 046

- [x] Known-symbol lookup delegates to `FixedMarketData`.
- [x] Unknown-symbol behavior preserves the existing `ValueError`.
- [x] Minor-unit output and failure semantics are tested.
- [x] No fallback or freshness policy is introduced.
