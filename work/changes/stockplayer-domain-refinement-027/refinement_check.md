# Refinement check

Slice 027 confirms repeated sell fills preserve both remaining ownership and
reservation state while settling proceeds incrementally.

The next refinement can add order-status projections or prepare the model EGD
packet from the now-coherent deterministic core.
