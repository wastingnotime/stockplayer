# Refinement check

Slice 015 establishes market availability as a separate replayable state
machine. Terminal close behavior and transition history are explicit before
session policy is connected to order acceptance.

The next refinement can enforce open-session requirements at the application
boundary and add session observations to the runtime scenario.
