# Refinement check

Slice 028 makes existing order facts visible as lifecycle read models without
adding redundant events. Partial sell and rejected reservation states are
reconstructed with exact remaining quantities.

The next refinement can add runtime order-timeline observations or prepare a
released adapter contract.
