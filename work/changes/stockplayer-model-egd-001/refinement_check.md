# Refinement check

EGD accepted the deterministic core boundary and documented the remaining
technology and deferred-domain gaps. The model returns to refinement only for a
visible lesson or an explicitly selected adapter contract.
