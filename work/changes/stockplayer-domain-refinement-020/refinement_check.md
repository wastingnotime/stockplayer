# Refinement check

Slice 020 establishes a versioned strategy boundary. V1 and V2 process the
same request and expose a deliberate full-fill versus liquidity-capped
difference without controlling account state.

The next refinement can expose engine comparison decisions as runtime evidence
or add an explicit candidate-validation scenario.
