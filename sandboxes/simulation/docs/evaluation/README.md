# Evaluation

Use headless deterministic checks during slice refinement. Preserve JSONL
observations under `runs/` only for a bounded evidence or model-release run.
Model EGD is deferred until the intended model scope is coherent.
