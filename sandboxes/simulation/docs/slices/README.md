# Simulation slices

Each slice document records the implementation pack, runtime target,
architecture mode, discovery scope, use-case contract, business rules, ports,
deterministic tests, scenario, done criteria, and exclusions. Slices extend one
shared environment; they are not separate applications.
